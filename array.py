# Creating a list (Python's version of an array)
numbers = [1, 2, 3, 4, 5]

# Accessing elements
print(numbers[0])  # Output: 1

# Adding elements
numbers.append(6)

# Removing elements
numbers.remove(2)

# Slicing
print(numbers[1:4])

# Looping
for num in numbers:
    print(num)
